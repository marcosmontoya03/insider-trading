######## USER INPUT ##########

one_stock <- function(ticker,
                      date,
                      pre_period, 
                      post_period,
                      sector){
  #Confirm that ticker in our data
  
  #If not, throw an error
  
  
  get_prices_data()
  
}

whole_data_results <- funciton(){
  
}

one_stock(ticker = "CNA", pretrend,  )

whole_data_



#' Get Price Data
#' 
#' @param type Specifies if the search is for a stock that experienced insider
#' trading, if it is a sector control search,
scrape_price_data <- function(ticker,
                              pre_period,
                              post_period,
                              type)